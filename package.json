{
  "name": "neveragain",
  "version": "1.0.0",
  "description": "Never Again forget the climate criminals are, starting with Siemens #StopAdani.",
  "main": "src/neverAgain.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "clean": "rm -rf dist/",
    "rename": "mv dist/neverAgain.*.js dist/neverAgain.js",
    "move": "cp src/manifest.json dist/manifest.json && cp src/neverAgain.css dist/neverAgain.css && mkdir dist/icons && cp icons/na-48x48-optimized.svg dist/icons/na-48x48-optimized.svg && cp src/privacy_policy.txt dist/privacy_policy.txt",
    "dev": "parcel src/index.html --no-source-maps",
    "build": "npm run clean && parcel build src/index.html --no-source-maps && npm run rename && npm run move"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pablooliva/neveragain.git"
  },
  "author": "pablo oliva",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pablooliva/neveragain/issues"
  },
  "homepage": "https://github.com/pablooliva/neveragain#readme",
  "dependencies": {
    "@popperjs/core": "^2.0.4",
    "mark.js": "^8.11.1"
  },
  "devDependencies": {
    "@types/mark.js": "^8.11.5",
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.7.5",
    "web-ext-types": "^3.2.1"
  }
}
